---
title: "Issue:Execute"
description: "并行执行队列中的 Issue（可选 worktree 隔离）。"
category: "workflows"
locale: "zh"
order: 22
---

# Issue:Execute

并行执行队列中的 Issue（可选 worktree 隔离）。

## 概览

| 属性 | 值 |
|----------|-------|
| **ID** | `issue-execute` |
| **类型** | Issue 管理 |
| **复杂度** | 高 |

## 用法

```bash
/issue:execute
```

## 描述

并行执行队列中的 Issue（可选 worktree 隔离）。
