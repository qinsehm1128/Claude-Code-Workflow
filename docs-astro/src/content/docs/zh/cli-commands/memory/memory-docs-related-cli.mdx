---
title: "/memory:docs-related-cli"
description: 为 git 变更模块生成 CLI 文档
category: cli-commands
order: 5
locale: zh
---

# /memory:docs-related-cli

使用批量 Agent 执行和自动工具降级，为受 git 变更影响的模块生成 CLI 文档。

## 概述

`/memory:docs-related-cli` 命令仅为受近期 git 变更影响的模块生成文档，为日常开发提供更快的文档更新。

**参数**：
- `--tool <gemini|qwen|codex>`：主要工具（默认：gemini）

**执行流程**：
1. 变更检测 → 2. 计划展示 → 3. 批量生成 → 4. 验证

## 特性

- **变更模块检测** - 使用 git diff 识别受影响的模块
- **智能批处理** - 按深度分组模块（4 个模块/Agent）
- **自动降级** - gemini→qwen→codex 失败时自动降级
- **深度顺序** - 处理深度 N→0，深度内批次并行
- **智能过滤** - 自动检测并跳过 tests/build/config/docs
- **单一策略** - 使用单层文档以提高速度

## 用法

```bash
# 为变更模块生成文档
/memory:docs-related-cli

# 使用特定工具
/memory:docs-related-cli --tool qwen
```

## 工具降级层次

```javascript
--tool gemini  →  [gemini, qwen, codex]  // default
--tool qwen    →  [qwen, gemini, codex]
--tool codex   →  [codex, gemini, qwen]
```

## 执行流程

### 阶段 1：变更检测与分析

```javascript
// Detect changed modules
Bash({command: "ccw tool exec detect_changed_modules '{\"format\":\"list\"}'", run_in_background: false});

// Cache git changes
Bash({command: "git add -A 2>/dev/null || true", run_in_background: false});
```

**解析输出** `depth:N|path:<PATH>|change:<TYPE>` 以提取受影响的模块。

**智能过滤**：基于项目技术栈自动检测并跳过 tests/build/config/docs。

**降级**：如未检测到变更，使用最近的模块（按深度前 10 个）。

### 阶段 2：计划展示

- 解析 `--tool`（默认：gemini）
- 刷新代码索引以确保准确的变更检测
- 检测变更模块
- **智能过滤模块**（自动检测技术栈，跳过 tests/build/config）
- 缓存 git 变更
- 无变更时应用降级
- 构建工具降级顺序
- **展示过滤后的计划** 包含变更类型
- **等待 y/n 确认**

### 阶段 3：批量文档生成

```javascript
let modules_by_depth = group_by_depth(changed_modules);
let tool_order = construct_tool_order(primary_tool);

for (let depth of sorted_depths.reverse()) {  // N → 0
  let batches = batch_modules(modules_by_depth[depth], 4);

  for (let batch of batches) {
    let parallel_tasks = batch.map(module => {
      return async () => {
        for (let tool of tool_order) {
          Bash({
            command: `cd ${module.path} && ccw tool exec generate_module_docs '{"strategy":"single","sourcePath":".","projectName":"${project_name}","tool":"${tool}"}'`,
            run_in_background: false
          });
          if (bash_result.exit_code === 0) {
            report(`✅ ${module.path} docs generated with ${tool}`);
            return true;
          }
        }
        report(`❌ FAILED: ${module.path} failed all tools`);
        return false;
      };
    });
    await Promise.all(parallel_tasks.map(task => task()));
  }
}
```

### 阶段 4：验证

- 验证文档文件已创建
- 显示统计数据
- 展示生成文档的摘要

## 策略选择

**关联模式** 使用 `single` 策略：
- 每个模块生成单一文档
- 比全量模式的多层方法更快
- 适合迭代开发

## 与全量 CLI 文档的对比

| 方面 | 关联文档 | 全量文档 |
|--------|--------------|-----------|
| **范围** | 仅变更模块 | 所有项目模块 |
| **速度** | 快（分钟级） | 较慢（10-30 分钟） |
| **使用场景** | 日常开发 | 大规模重构 |
| **策略** | 仅 single | 深度>=3 使用 full |
| **触发时机** | 提交后 | 重大变更后 |
| **批处理** | 4 模块/Agent | 4 模块/Agent |
| **降级** | gemini→qwen→codex | gemini→qwen→codex |

## 示例

### 基本用法

```bash
# 提交后为变更模块生成文档
/memory:docs-related-cli

# Output:
# Detecting git changes...
# Found 8 changed modules
# Filtered: 3 test modules skipped
# Plan: Generate docs for 5 modules
# Confirm? (y/n): y
#
# Depth 3: [4/4] ✅
# Depth 2: [1/1] ✅
# Summary: 5/5 modules documented
```

### 工具选择

```bash
# 使用 Qwen 生成
/memory:docs-related-cli --tool qwen
```

## 相关命令

- **/memory:docs-full-cli** - 为所有模块生成文档
- **/memory:update-related** - 更新变更模块的 CLAUDE.md
- **/memory:compact** - 压缩会话内存

## 注意事项

- **智能过滤** 自动跳过 test/build/config 目录
- **变更检测** 使用 git diff 查找受影响的模块
- **单一策略** 优化迭代开发的速度
- **工具降级** 确保即使主要工具失败也能完成
- **验证** 确认所有文档文件成功创建
